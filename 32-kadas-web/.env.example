# Leave SUBDOMAIN as is, I use this to replace it with the real subdomain -
# generated from the login email address.
# Feel free to change 'demos-test.ml' with the proper domain.
# Make sure to have /api at the end to point to the API endpoint.
API_ENDPOINT=http://SUBDOMAIN.demos-test.ml/api
